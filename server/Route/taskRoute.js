const express = require("express")
const route = express.Router();

const tstController = require("../Controllers/taskController");

// route.post("/taskCreate",tstController.createTask)
// route.get("/userDisplay",tstController.userdisplay)

// route.get("/taskDisplay",tstController.displayTasks)


module.exports = route;